This folder is the bot's growing memory. After each run it writes:
  history.csv      - one row per run (the outlook + signals), used to self-score
  failure_log.md   - past 'quiet' calls that were followed by a real flare (the lessons)
Both are created automatically on the first run.
